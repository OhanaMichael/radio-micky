# סיכום להעברה — רדיו מיקי (radio-micky.co.il)

> מסמך מסירה לשיחה חדשה שבה תחובר תיקיית קבצי האתר.
> תאריך: 17.08.2026

---

## פרטי הפרויקט

| | |
|---|---|
| אתר | https://www.radio-micky.co.il |
| ריפו | `OhanaMichael/radio-micky` — **ציבורי**, ענף `main` |
| אירוח | Netlify, auto-deploy מ-push |
| תיקייה מקומית | `C:\Users\micky\OneDrive\מסמכים\עסקי\ניהול פרוייקטים\אתר אינטרנט רדיו מיקי\קבצי אתר` |
| Google Analytics | `G-P9S5CGRGCL` |
| טלפון | 04-6717333 · שוק העירוני, יהודה הלוי 407, טבריה · מאז 1996 |

**מבנה האתר:** `index.html` + דפי שירות נפרדים — `/mobileye`, `/mobileye-service`, `/multimedia`, `/kodanit`, `/cobra`, `/migun`, `/dalkan`, `/proof`.

---

## ⛔ משימה פתוחה #1 — פריסת דף הקודניות (הכי דחוף)

**סטטוס:** הקובץ נערך ואומת במלואו, אך **לא הועתק למחשב ולא נדחף**. התיקייה המקומית עדיין מכילה את הגרסה הישנה.

### איך לזהות מה יושב על הדיסק

| | ישן | חדש |
|---|---|---|
| גודל `kodanit.html` | 35,285 בייט | **56,991 בייט** |
| מופעי "עלוקה" | 0 | 38 |
| מילים גלויות | ~700 | 1,964 |

### מה צריך לקרות

1. להחליף את `kodanit.html` ואת `sitemap.xml` בתיקייה המקומית
2. `git add kodanit.html sitemap.xml` → `git status` (חייב להראות 2 קבצים) → `commit` → `push`
3. הודעת קומיט: `SEO: הרחבת דף הקודניות - ניתוק קודן, עלוקה, אבחון תקלות, FAQ מורחב ו-Schema`

### אם צריך לשחזר את השינויים מאפס

בתיקייה הזו יש `apply-kodanit-seo.py` — סקריפט שמבצע את כל 16 השינויים על קובץ המקור. הקובץ המקורי זמין תמיד ב:
```
https://raw.githubusercontent.com/OhanaMichael/radio-micky/main/kodanit.html
```
הרצה: לשים את `kodanit.html` המקורי ליד הסקריפט ולהריץ `python apply-kodanit-seo.py`.
הסקריפט מוודא שכל מחרוזת יעד קיימת ויחידה לפני החלפה, ונעצר בשגיאה אם לא.

### 16 השינויים שבוצעו

| # | מה |
|---|---|
| 1 | **Title** → `קודן לרכב בטבריה — ניתוק, תיקון וחילוץ קודנית \| רדיו מיקי` |
| 2 | **Meta description** → פותח ב"קודן לרכב לא מגיב?" |
| 3–4 | og:title + og:description מיושרים לחדשים |
| 5 | **Meta keywords** → 22 ביטויים כולל ניתוק/ביטול/איפוס/עלוקה/אימובילייזר/כספת/רב-בריח/ספטרוטק |
| 6 | **H1** → `קודן לרכב בטבריה — ניתוק, תיקון, איפוס וחילוץ קודנית תקועה` |
| 7 | Hero sub נכתב מחדש סביב תסמינים |
| 8 | Hero badge → "קודן וקודנית לרכב" |
| 9 | **כרטיסי שירות 4 → 6** (נוספו "ניתוק וביטול קודן", "איפוס קודן שננעל") |
| 10 | **סקשן חדש:** טבלת אבחון תסמינים (6 שורות) — ל-Featured Snippet ולציטוט AI |
| 11 | **סקשן חדש:** ניתוק/ביטול קודן — מתי כן/לא + אזהרת מערכות חיוניות |
| 12 | **סקשן חדש:** עלוקה לרכב — הגדרה, חוטית/אלחוטית, כספת |
| 13 | **סקשן חדש:** מותגים (8 chips) + אזורי שירות (14 chips) |
| 14 | כותרת שלבים → "קודן תקוע או קודנית תקולה?" |
| 15 | **FAQ 5 → 16 שאלות** |
| 16 | **Schema נבנה מחדש:** Service + `hasOfferCatalog` (9) + `areaServed` (17) + FAQPage (14) + WebPage + Breadcrumb |

בנוסף: 2 כותרות H2 יושרו לאוצר המילים החדש, ו-`sitemap.xml` עודכן ל-`lastmod 2026-08-17`.

**עקרון עיצובי:** לא נוצר CSS חדש. הכל משתמש במחלקות שכבר קיימות בדף — `sec`, `sec-inner`, `sec-label`, `sec-title`, `prose`, `specs-table`, `chip-grid`, `chip`, `alert`, `feat`, `what-grid`, `what-feats`, `svc-card`, `faq-item`.

### אימות שבוצע לפני המסירה
JSON-LD נפרס תקין · תגיות מאוזנות (9 section, 16 details, 110 div) · H1 יחיד · 23/23 מילות מפתח מהניתוח מופיעות בטקסט הגלוי.

---

## הרקע — למה השינוי הזה

הדף דורג טוב על "חילוץ קודנית תקועה" אך לא הופיע על אף ביטוי סמוך. הסיבה: הוא נכתב באוצר מילים אחד בלבד — *קודנית + תקולה + חילוץ*. הגולשים בישראל מחפשים אחרת:

| בדף הישן | מה באמת מקלידים |
|---|---|
| קודנית | **קודן** (זכר) — נפוץ פי כמה |
| קודנית תקולה | **קודן לא מגיב** / **ננעל** / **לא עובד** |
| חילוץ | **ניתוק קודן** / **ביטול** / **איפוס** |
| — לא הופיע | **עלוקה** — הרכיב הנסתר שגורם לתקלה |
| — לא הופיע | **אימובילייזר**, **כספת**, **קודן מצפצף** |

מתוך ~19 אשכולות רלוונטיים: 3 היו מכוסים, 15 חסרו לגמרי. "ניתוק קודן לרכב" — הביטוי המסחרי החזק בנישה — לא הופיע אפילו פעם אחת, בעוד 8 מ-10 המתחרים בנויים סביבו.

**מתחרים שנותחו:** vs-key.co.il, midrag.co.il, autopower.co.il, kodan.co.il, israel-key.co.il, shay-sound.co.il, calliber.co.il, keyforme.co.il, amir-car.co.il, oransystems.co.il

**מילון מקצועי (בסיס לכל התוכן):** קודן/קודנית = לוח המקשים · עלוקה = היחידה הנסתרת שמנתקת את מעגל ההתנעה · כספת = תיבת אלומיניום 4–7 מ"מ על מחשב הרכב/העלוקה · אימובילייזר = השם הלועזי הכולל · מגעי פחם = הסיבה הנפוצה ביותר לתקלה.

מסמך הניתוח המלא: `kodanit-keyword-plan.md` — כבר יושב ב-Downloads.

---

## 📋 משימה פתוחה #2 — קישורים פנימיים ב-index.html

טקסטי העוגן בדף הבית עדיין אומרים "קודניות לרכב". גיוון שלהם יחזק את הדף החדש:

| מיקום ב-index.html | טקסט עוגן מוצע |
|---|---|
| כרטיס שירות "קודניות לרכב" | ניתוק, תיקון וחילוץ קודן לרכב ← |
| סקשן SEO תחתון | קודן לרכב לא מגיב? לחצו כאן |
| FAQ בדף הבית | איפה מנתקים או מתקנים קודן בטבריה? |
| `/migun` | קודן לרכב לדרישות הביטוח |
| `/cobra` | ניתוק קודן קוברה |

**לא בוצע.** דורש עריכת `index.html` ודפי שירות.

---

## 📋 משימה פתוחה #3 — אחרי הפריסה

- Search Console → Request Indexing ל-`/kodanit` → הגשת `sitemap.xml` מחדש
- בדיקת Schema: https://search.google.com/test/rich-results
- Google Business Profile → הוספת שירותים: ניתוק קודן, תיקון קודן, איפוס קודן, חילוץ רכב תקוע
- **מדידה בעוד 3 שבועות:** Search Console → Performance → Pages → סנן `/kodanit` → Queries. לחפש הופעות חדשות על `ניתוק קודן`, `קודן לא מגיב`, `קודן ננעל`, `עלוקה`, `איפוס קודן`.
  הופעות בלי קליקים → לשפר Title/Description. אין הופעות → לבקש אינדוקס מחדש.

---

## החלטות שהתקבלו במפורש — לא לשנות בלי דיון

1. **אין מחירים מספריים בדף.** מתחרים מפרסמים טווחים וזה מושך תנועה, אך זו הוחלטה כהחלטה עסקית פתוחה.
2. **המשפט "השירות ניתן לבעל הרכב הרשום בלבד, בכפוף להצגת רישיון רכב ותעודת זהות" — לא להסיר.** ביטויים כמו "עקיפת קודן" מושכים גם חיפושים בעייתיים; ההבהרה מגנה על הדף ונכונה עסקית.
3. **לא ליצור עדיין** דפי נחיתה נפרדים `/nituk-kodan`, `/aluka`, `/kodan-lo-megiv`. להמתין 4–6 שבועות ולראות איך הדף הראשי מתפקד.

---

## תוספות שנתבקשו ובוצעו

- מותגי קודנים: נוספו **רב-בריח** ו**ספטרוטק** (לצד איתוראן, פוינטר, רנג'ר, Inet, קוברה, סקיילוק)
- אזורי שירות: נוספו **סובב כינרת**, **עמק הירדן**, **רמת הגולן**
- אזהרת ניתוק עצמאי: הורחבה מ"סטרטר, משאבת דלק" ל-**אימובילייזר מקורי של היצרן, מחשב הרכב (ECU), רשת CAN Bus** ומערכת האיתור

---

## ⚠️ נושא נפרד — index.html ו-EmailJS

בחלק מוקדם של השיחה טופל `index.html` (טופס יצירת קשר + מובילאיי). **הסטטוס שם לא ודאי ודורש בדיקה לפני שנוגעים.**

### אזהרה חשובה
קובץ `index.html` שהועלה לשיחה **היה קטוע** — ה-JavaScript נחתך באמצע `IntersectionObserver` והדף לא נטען כלל. הסוף הושלם ידנית. **לפני כל עריכה של index.html — לוודא שהוא נגמר ב-`</script></body></html>` תקין.**

### הגדרות EmailJS הסופיות (חשבון אחד)
```
Public Key:            fTjbYldj3Zug1Hm_M
Service ID:            Radio-Micky
Template בדיקת זכאות:  template_6tk6oi7
Template יצירת קשר:    template_8ewwmn5
```
**הבעיה שאותרה:** ערבוב שני חשבונות EmailJS — `emailjs.init()` מאתחל חשבון אחד בלבד, ולכן Service מחשבון אחד + Template מחשבון אחר גורם לכשל 400 ולנפילה ל-fallback של mailto. **כל הארבעה חייבים להיות מאותו חשבון.**

הגרסה החיה של האתר כן מציגה טופס יצירת קשר מלא (שם, טלפון, אימייל, סוג רכב, שנת ייצור, נושא, הודעה) — כלומר חלק מהעבודה כבר בפרודקשן. יש להשוות מול הריפו לפני שינויים.

---

## איך להתחיל את השיחה החדשה

חבר את התיקייה, ואז:

> "מחובר לתיקיית האתר של רדיו מיקי. קרא את HANDOFF.md, ובצע את משימה #1 — החלף את kodanit.html ו-sitemap.xml בגרסאות המעודכנות ואמת שהגודל 56,991."

הקבצים המוכנים לפריסה נמצאים באותה תיקייה: `kodanit.html`, `sitemap.xml`.
